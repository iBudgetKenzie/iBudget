import styled from "styled-components";

export const MainConteinerMobile = styled.div`
    display: flex;
    justify-content: center;
    padding: 0px 15px;
    
    height: calc(100vh - 67px);
    width: 100vw;
    background: linear-gradient(
        rgba(var(--color-purple), 1) 50%, rgba(var(--color-black), 1) 50%
    );
`